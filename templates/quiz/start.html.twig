{% extends 'quiz/base.html.twig' %}

{% block title %}Démarrer le Quiz{% endblock %}

{% block styles %}
<style>
    .start-container {
        max-width: 600px;
        margin: 3rem auto;
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-label {
        display: block;
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: var(--dark);
    }

    .form-input {
        width: 100%;
        padding: 0.75rem;
        border: 2px solid var(--light);
        border-radius: 10px;
        font-size: 1rem;
        transition: border-color 0.3s;
    }

    .form-input:focus {
        outline: none;
        border-color: var(--primary);
    }

    .info-box {
        background: #eff6ff;
        border-left: 4px solid var(--primary);
        padding: 1rem;
        margin: 2rem 0;
        border-radius: 5px;
    }
</style>
{% endblock %}

{% block body %}
<div class="container">
    <div class="start-container">
        <div class="card">
            <h1 style="text-align: center; color: var(--primary);">📝 Commençons !</h1>
            
            <div class="info-box">
                <strong>ℹ️ À propos du quiz :</strong>
                <ul style="margin-top: 0.5rem; margin-left: 1.5rem;">
                    <li>12 questions rapides</li>
                    <li>Durée : 5-7 minutes</li>
                    <li>Résultats instantanés</li>
                </ul>
            </div>

            <form method="get" action="{{ path('app_quiz_questions') }}">
                <div class="form-group">
                    <label class="form-label" for="nom">Nom</label>
                    <input type="text" id="nom" name="nom" class="form-input" placeholder="Votre nom" required>
                </div>

                <div class="form-group">
                    <label class="form-label" for="prenom">Prénom</label>
                    <input type="text" id="prenom" name="prenom" class="form-input" placeholder="Votre prénom" required>
                </div>

                <div class="form-group">
                    <label class="form-label" for="email">Email</label>
                    <input type="email" id="email" name="email" class="form-input" placeholder="votre.email@example.com">
                </div>

                <div class="form-group">
                    <label class="form-label" for="niveau">Niveau d'études</label>
                    <select id="niveau" name="niveau" class="form-input" required>
                        <option value="">-- Sélectionnez --</option>
                        <option value="Bac">Bac</option>
                        <option value="Bac+1">Bac+1</option>
                        <option value="Bac+2">Bac+2</option>
                        <option value="Licence">Licence (Bac+3)</option>
                        <option value="Master">Master (Bac+5)</option>
                    </select>
                </div>

                <button type="submit" class="btn btn-primary" style="width: 100%; margin-top: 1rem;">
                    Démarrer le Quiz 🚀
                </button>
            </form>

            <div style="text-align: center; margin-top: 1rem;">
                <a href="{{ path('app_quiz_index') }}" class="btn btn-secondary">← Retour</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
