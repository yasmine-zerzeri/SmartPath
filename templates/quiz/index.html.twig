{% extends 'quiz/base.html.twig' %}

{% block title %}Accueil Quiz{% endblock %}

{% block styles %}
<style>
    .hero {
        text-align: center;
        padding: 3rem 0;
    }

    .hero h1 {
        font-size: 3rem;
        color: white;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
    }

    .hero p {
        font-size: 1.3rem;
        color: rgba(255,255,255,0.9);
        margin-bottom: 2rem;
    }

    .features {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin: 3rem 0;
    }

    .feature-card {
        background: white;
        padding: 2rem;
        border-radius: 15px;
        text-align: center;
        box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    }

    .feature-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
    }

    .feature-title {
        font-size: 1.2rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
    }

    .cta-section {
        text-align: center;
        margin-top: 3rem;
    }
</style>
{% endblock %}

{% block body %}
<div class="container">
    <div class="hero">
        <h1>🎯 Quiz de Personnalité</h1>
        <p>Découvrez quelle spécialité informatique correspond le mieux à votre profil !</p>
    </div>

    <div class="features">
        <div class="feature-card">
            <div class="feature-icon">🤖</div>
            <div class="feature-title">10 Spécialités</div>
            <p>Data Science, Cloud, IoT, Cybersécurité...</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">📊</div>
            <div class="feature-title">Analyse Précise</div>
            <p>Algorithme intelligent d'analyse de profil</p>
        </div>
        <div class="feature-card">
            <div class="feature-icon">🎓</div>
            <div class="feature-title">Recommandations</div>
            <p>Suggestions de filières adaptées à vous</p>
        </div>
    </div>

    <div class="cta-section">
        <a href="{{ path('app_quiz_start') }}" class="btn btn-primary" style="font-size: 1.2rem; padding: 1rem 3rem;">
            Commencer le Quiz 🚀
        </a>
    </div>
</div>
{% endblock %}
