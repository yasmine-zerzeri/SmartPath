<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}SmartPath - Votre plateforme d'apprentissage{% endblock %}</title>
    
    {% block stylesheets %}
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
            transition: background 0.5s ease;
        }
        
        /* Dark Mode (Default) */
        body {
            background: linear-gradient(135deg, #0a1628 0%, #1e3a5f 50%, #2563eb 100%);
        }
        
        /* Light Mode */
        body.light-mode {
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 50%, #bae6fd 100%);
        }
        
        /* Space Background */
        .space-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 0;
            pointer-events: none;
            transition: opacity 0.5s ease;
        }
        
        body.light-mode .space-background {
            opacity: 0.4;
        }
        
        .star {
            position: absolute;
            background: white;
            border-radius: 50%;
            animation: twinkle-star 3s infinite ease-in-out;
        }
        
        body.light-mode .star {
            display: none;
        }
        
        @keyframes twinkle-star {
            0%, 100% { opacity: 0.5; transform: scale(1); box-shadow: 0 0 2px white; }
            25% { opacity: 1; transform: scale(1.5); box-shadow: 0 0 10px white, 0 0 20px rgba(255, 255, 255, 0.5); }
            50% { opacity: 0.6; transform: scale(1); box-shadow: 0 0 5px white; }
            75% { opacity: 0.9; transform: scale(1.3); box-shadow: 0 0 8px white, 0 0 15px rgba(255, 255, 255, 0.3); }
        }
        
        .star-pulse {
            animation: pulse-star 2s infinite ease-in-out;
        }
        
        @keyframes pulse-star {
            0%, 100% { opacity: 0.8; transform: scale(1); box-shadow: 0 0 5px #fff, 0 0 10px #60a5fa; }
            50% { opacity: 1; transform: scale(2); box-shadow: 0 0 15px #fff, 0 0 30px #60a5fa, 0 0 40px #93c5fd; }
        }
        
        .planet {
            position: absolute;
            border-radius: 50%;
            animation: float-planet 20s infinite ease-in-out;
        }
        
        .planet:nth-child(1) {
            width: 100px; height: 100px;
            background: radial-gradient(circle at 30% 30%, #60a5fa, #1e40af);
            left: 10%; top: 20%; opacity: 0.3;
            box-shadow: 0 0 40px rgba(96, 165, 250, 0.5);
        }
        
        body.light-mode .planet:nth-child(1) {
            background: radial-gradient(circle at 30% 30%, #7dd3fc, #0284c7);
            opacity: 0.5;
        }
        
        .planet:nth-child(2) {
            width: 150px; height: 150px;
            background: radial-gradient(circle at 30% 30%, #93c5fd, #3b82f6);
            right: 15%; top: 50%; opacity: 0.2;
            animation-delay: 3s;
            box-shadow: 0 0 60px rgba(147, 197, 253, 0.4);
        }
        
        body.light-mode .planet:nth-child(2) {
            background: radial-gradient(circle at 30% 30%, #bae6fd, #0ea5e9);
            opacity: 0.4;
        }
        
        .planet:nth-child(3) {
            width: 80px; height: 80px;
            background: radial-gradient(circle at 30% 30%, #bfdbfe, #60a5fa);
            left: 60%; bottom: 20%; opacity: 0.25;
            animation-delay: 6s;
            box-shadow: 0 0 30px rgba(191, 219, 254, 0.5);
        }
        
        body.light-mode .planet:nth-child(3) {
            background: radial-gradient(circle at 30% 30%, #e0f2fe, #38bdf8);
            opacity: 0.5;
        }
        
        @keyframes float-planet {
            0%, 100% { transform: translateY(0) translateX(0); }
            25% { transform: translateY(-30px) translateX(20px); }
            50% { transform: translateY(-60px) translateX(-10px); }
            75% { transform: translateY(-30px) translateX(-30px); }
        }
        
        .nebula {
            position: absolute;
            width: 500px; height: 500px;
            background: radial-gradient(circle, rgba(59, 130, 246, 0.4) 0%, rgba(96, 165, 250, 0.2) 40%, transparent 70%);
            border-radius: 50%;
            filter: blur(80px);
            animation: nebula-pulse 10s infinite alternate;
        }
        
        body.light-mode .nebula {
            background: radial-gradient(circle, rgba(14, 165, 233, 0.3) 0%, rgba(56, 189, 248, 0.15) 40%, transparent 70%);
        }
        
        .nebula:nth-child(1) { top: 10%; left: 20%; }
        .nebula:nth-child(2) { bottom: 20%; right: 10%; animation-delay: 3s; }
        
        @keyframes nebula-pulse {
            0% { transform: scale(1); opacity: 0.4; }
            100% { transform: scale(1.5); opacity: 0.7; }
        }
        
        /* Navbar */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: rgba(10, 22, 40, 0.95);
            backdrop-filter: blur(15px);
            border-bottom: 1px solid rgba(96, 165, 250, 0.3);
            box-shadow: 0 4px 30px rgba(37, 99, 235, 0.3);
            padding: 1.2rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            animation: slideDown 0.5s ease-out;
            transition: all 0.5s ease;
        }
        
        body.light-mode .navbar {
            background: rgba(255, 255, 255, 0.98);
            border-bottom: 2px solid rgba(3, 105, 161, 0.2);
            box-shadow: 0 2px 20px rgba(14, 165, 233, 0.15);
        }
        
        @keyframes slideDown {
            from { transform: translateY(-100%); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        
        .navbar-logo {
            display: flex;
            align-items: center;
            gap: 1rem;
            text-decoration: none;
        }
        
        .navbar-logo img {
            height: 50px;
            width: auto;
            filter: drop-shadow(0 0 15px rgba(96, 165, 250, 0.7));
            transition: filter 0.5s ease;
        }
        
        body.light-mode .navbar-logo img {
            filter: drop-shadow(0 0 15px rgba(14, 165, 233, 0.5));
        }
        
        .navbar-logo h1 {
            background: linear-gradient(135deg, #60a5fa 0%, #93c5fd 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-size: 1.8rem;
            font-weight: 700;
            filter: drop-shadow(0 0 20px rgba(96, 165, 250, 0.5));
            transition: all 0.5s ease;
        }
        
        body.light-mode .navbar-logo h1 {
            background: linear-gradient(135deg, #0284c7 0%, #0369a1 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 0 20px rgba(14, 165, 233, 0.3));
        }
        
        .navbar-menu {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            list-style: none;
        }
        
        .navbar-menu a {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.8rem 1.5rem;
            color: #bfdbfe;
            text-decoration: none;
            font-weight: 500;
            border-radius: 25px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        body.light-mode .navbar-menu a {
            color: #0c4a6e;
            font-weight: 600;
        }
        
        .navbar-menu a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
            transition: left 0.3s ease;
            z-index: -1;
            border-radius: 25px;
        }
        
        body.light-mode .navbar-menu a::before {
            background: linear-gradient(135deg, #0ea5e9 0%, #38bdf8 100%);
        }
        
        .navbar-menu a:hover::before,
        .navbar-menu a.active::before {
            left: 0;
        }
        
        .navbar-menu a:hover,
        .navbar-menu a.active {
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(37, 99, 235, 0.6);
        }
        
        .navbar-menu a .icon {
            font-size: 1.3rem;
        }
        
        .navbar-actions {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }
        
        /* Theme Toggle */
        .theme-toggle {
            position: relative;
            width: 70px;
            height: 34px;
            background: rgba(37, 99, 235, 0.3);
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            border: 2px solid rgba(96, 165, 250, 0.5);
        }
        
        body.light-mode .theme-toggle {
            background: rgba(14, 165, 233, 0.2);
            border-color: rgba(3, 105, 161, 0.5);
        }
        
        .theme-toggle-slider {
            position: absolute;
            top: 3px;
            left: 3px;
            width: 24px;
            height: 24px;
            background: linear-gradient(135deg, #60a5fa 0%, #93c5fd 100%);
            border-radius: 50%;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8rem;
            box-shadow: 0 2px 10px rgba(96, 165, 250, 0.5);
        }
        
        body.light-mode .theme-toggle-slider {
            left: calc(100% - 27px);
            background: linear-gradient(135deg, #f59e0b 0%, #fbbf24 100%);
            box-shadow: 0 2px 10px rgba(245, 158, 11, 0.5);
        }
        
        /* Mobile Menu Toggle */
        .mobile-menu-toggle {
            display: none;
            font-size: 1.5rem;
            color: #60a5fa;
            cursor: pointer;
            padding: 0.5rem;
        }
        
        body.light-mode .mobile-menu-toggle {
            color: #0284c7;
        }
        
        /* Logout Button */
        .logout-btn {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.8rem 1.2rem;
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            color: white;
            text-decoration: none;
            font-weight: 600;
            border-radius: 25px;
            transition: all 0.3s ease;
            border: 2px solid rgba(239, 68, 68, 0.5);
            cursor: pointer;
            font-size: 0.95rem;
        }
        
        .logout-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(239, 68, 68, 0.6);
            background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
        }
        
        body.light-mode .logout-btn {
            background: linear-gradient(135deg, #f87171 0%, #ef4444 100%);
            border-color: rgba(239, 68, 68, 0.3);
        }
        
        body.light-mode .logout-btn:hover {
            background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
        }
        
        /* Container */
        .container {
            position: relative;
            z-index: 1;
            padding-top: 90px;
            max-width: 1400px;
            margin: 0 auto;
            padding-left: 2rem;
            padding-right: 2rem;
        }
        
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* Responsive */
        @media (max-width: 968px) {
            .navbar-menu {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                right: 0;
                background: rgba(10, 22, 40, 0.98);
                backdrop-filter: blur(15px);
                flex-direction: column;
                padding: 1rem;
                gap: 0.5rem;
                border-top: 1px solid rgba(96, 165, 250, 0.3);
            }
            
            body.light-mode .navbar-menu {
                background: rgba(255, 255, 255, 0.98);
                border-top: 1px solid rgba(3, 105, 161, 0.3);
            }
            
            .navbar-menu.active {
                display: flex;
            }
            
            .navbar-menu a {
                width: 100%;
                justify-content: center;
            }
            
            .mobile-menu-toggle {
                display: block;
            }
            
            .navbar-actions {
                gap: 1rem;
            }
        }
        
        @media (max-width: 768px) {
            .navbar {
                padding: 1rem;
            }
            
            .navbar-logo h1 {
                font-size: 1.4rem;
            }
            
            .container {
                padding-left: 1rem;
                padding-right: 1rem;
            }
        }
    </style>
    {% endblock %}
</head>
<body>
    <div class="space-background" id="space">
        <div class="planet"></div>
        <div class="planet"></div>
        <div class="planet"></div>
        <div class="nebula"></div>
        <div class="nebula"></div>
    </div>
    
    <nav class="navbar">
        <a href="{{ path('public_home') }}" class="navbar-logo">
            <img src="{{ asset('images/logo.png') }}" alt="SmartPath Logo">
            <h1>SmartPath</h1>
        </a>
        
        <div class="mobile-menu-toggle" onclick="toggleMobileMenu()">☰</div>
        
        <ul class="navbar-menu" id="navbar-menu">
            <li>
                <a href="{{ path('public_home') }}" class="{% if app.request.get('_route') == 'public_home' %}active{% endif %}">
                    <span class="icon">🏠</span>
                    <span>Accueil</span>
                </a>
            </li>
            <li>
                <a href="{{ path('public_courses') }}" class="{% if app.request.get('_route') == 'public_courses' %}active{% endif %}">
                    <span class="icon">📚</span>
                    <span>Cours</span>
                </a>
            </li>
            <li>
                <a href="{{ path('public_quiz') }}" class="{% if app.request.get('_route') == 'public_quiz' %}active{% endif %}">
                    <span class="icon">📝</span>
                    <span>Quiz</span>
                </a>
            </li>
            <li>
                <a href="{{ path('public_stage') }}" class="{% if app.request.get('_route') == 'public_stage' %}active{% endif %}">
                    <span class="icon">🎓</span>
                    <span>Stages</span>
                </a>
            </li>
            <li>
                <a href="{{ path('public_contact') }}" class="{% if app.request.get('_route') == 'public_contact' %}active{% endif %}">
                    <span class="icon">📧</span>
                    <span>Contact</span>
                </a>
            </li>
            <li>
                <a href="{{ path('public_about') }}" class="{% if app.request.get('_route') == 'public_about' %}active{% endif %}">
                    <span class="icon">ℹ️</span>
                    <span>À Propos</span>
                </a>
            </li>
        </ul>
        
        <div class="navbar-actions">
            <a href="{{ path('logout') }}" class="logout-btn">
                <span>🚪</span>
                <span>Se déconnecter</span>
            </a>
            <div class="theme-toggle" onclick="toggleTheme()">
                <div class="theme-toggle-slider">
                    <span id="theme-icon">🌙</span>
                </div>
            </div>
        </div>
    </nav>
    
    <div class="container">
        {% block body %}{% endblock %}
    </div>
    
    {% block javascripts %}
    <script>
        function createStars() {
            const spaceContainer = document.getElementById('space');
            const starCount = 200;
            
            for (let i = 0; i < starCount; i++) {
                const star = document.createElement('div');
                star.className = Math.random() > 0.7 ? 'star star-pulse' : 'star';
                
                const size = Math.random() * 3 + 1;
                star.style.width = size + 'px';
                star.style.height = size + 'px';
                star.style.left = Math.random() * 100 + '%';
                star.style.top = Math.random() * 100 + '%';
                star.style.animationDelay = Math.random() * 3 + 's';
                star.style.animationDuration = (Math.random() * 2 + 2) + 's';
                
                spaceContainer.appendChild(star);
            }
        }
        
        createStars();
        
        function toggleTheme() {
            const body = document.body;
            const themeIcon = document.getElementById('theme-icon');
            
            body.classList.toggle('light-mode');
            
            if (body.classList.contains('light-mode')) {
                themeIcon.textContent = '☀️';
                localStorage.setItem('theme', 'light');
            } else {
                themeIcon.textContent = '🌙';
                localStorage.setItem('theme', 'dark');
            }
        }
        
        function toggleMobileMenu() {
            const menu = document.getElementById('navbar-menu');
            menu.classList.toggle('active');
        }
        
        window.addEventListener('DOMContentLoaded', () => {
            const savedTheme = localStorage.getItem('theme');
            const themeIcon = document.getElementById('theme-icon');
            
            if (savedTheme === 'light') {
                document.body.classList.add('light-mode');
                themeIcon.textContent = '☀️';
            }
        });
        
        // Close mobile menu when clicking outside
        document.addEventListener('click', function(event) {
            const menu = document.getElementById('navbar-menu');
            const toggle = document.querySelector('.mobile-menu-toggle');
            const navbar = document.querySelector('.navbar');
            
            if (!navbar.contains(event.target) && menu.classList.contains('active')) {
                menu.classList.remove('active');
            }
        });
    </script>
    {% endblock %}
</body>
</html>
